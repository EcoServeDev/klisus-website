[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every unknown path should serve the built index.html so
# react-router-dom's BrowserRouter can take over (otherwise /about, /products
# etc. return 404 when hit directly).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
